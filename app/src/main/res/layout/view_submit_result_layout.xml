<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="100dp"
    android:layout_height="100dp"
    android:background="@drawable/app_evaluate_suggestion_bg"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/rl_submit_result_container"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_weight="1.3"
        android:gravity="center" >

        <ProgressBar
            android:id="@+id/pb_submit_result_progressbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:indeterminateDrawable="@drawable/submit_loading_drawable"
            android:indeterminateDuration="1500"/>

        <ImageView
            android:id="@+id/iv_submit_result_success"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@mipmap/submit_result_success"
            android:visibility="gone" />
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_submit_result_status"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_weight="0.7"
        android:text="@string/submit_resut_ing"
        android:textColor="@color/black"
        android:textSize="@dimen/common_listItem_textSize" />

</LinearLayout>